# Copie para .env e ajuste para o IP da sua máquina na rede local.
# No celular físico com Expo Go, "localhost" aponta para o próprio celular —
# use o IP da máquina rodando o backend Django (ex.: 192.168.0.10).
EXPO_PUBLIC_API_URL=http://192.168.0.10:8000/api
